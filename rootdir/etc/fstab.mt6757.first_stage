# First-stage init mounts the existing SAR filesystem and switches root to it.
# Vendor is a directory inside system on this device, not a separate partition.
/dev/block/platform/mtk-msdc.0/11230000.msdc0/by-name/system /system ext4 ro wait,first_stage_mount
